specVersion: "v1"
serviceId: "Axiom"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "axiom-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
